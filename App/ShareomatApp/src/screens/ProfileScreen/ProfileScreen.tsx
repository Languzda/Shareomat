import { Text } from "react-native";
import { ProfileScreenPropsType } from "../../types/ProfileScreenPropsType";

function ProfileScreen({navigation, route}: ProfileScreenPropsType): React.JSX.Element {
    return (
        <Text>Profil</Text>
    );
}

export default ProfileScreen;