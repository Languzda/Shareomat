/*const express = require("express")
const routes = require("../controllers/controller.ts")

const router = express.Router()

router.get("/hello", routes.get_hello)

router.get("/users", routes.get_users)
router.post("/users", routes.create_user)

router.get("offers", routes.get_offers)


module.exports = router*/